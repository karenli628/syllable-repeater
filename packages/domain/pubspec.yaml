# AI-Generate
# 純 Dart 領域層（M5）：禁止依賴 Flutter / sidecar / 平台 API。
# 依賴白名單由 CI 檢查（task-split 8.1，AT-09-02）。
name: domain
description: Syllable Repeater Domain Layer (pure Dart, M5)
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
