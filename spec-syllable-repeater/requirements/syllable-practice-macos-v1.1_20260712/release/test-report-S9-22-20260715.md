// AI-Generate
# S9-22 效能、捲動與暫存衛生測試報告（2026-07-15）

## 範圍

- 段落校正增刪／拖曳切點先更新畫面，Prosody 背景分析並以 generation 阻止舊結果倒灌。
- 移除全 App 第三層捲動，只保留功能頁與自由排列列區；最後一列捲到底後不回彈。
- 來源積木支援「拿起→放開觸控板捲動→點藍色位置放下」，並保留原本近距離拖曳。
- Whisper、Demucs、練習預覽與 v3 課程解包納入 session temp 生命週期，不刪使用者保存檔。

## TDD 證據

- Editor controller 新測試先因 `ProsodyAnalysisRunner`／provider 不存在而編譯紅燈；實作 isolate runner 與 generation guard 後，18/18 PASS。
- Responsive layout 測試先偵測到兩個全域 scrollbar；Arrangement 20 列測試先找不到拿起／放下提示，實作後轉綠。
- Infra 測試先因 `ManagedTempSession` 不存在、Whisper JSON 與 Demucs workDir 殘留而紅燈；新增 lease 鎖與 finally 後轉綠。
- 課程解包故障注入先確認後續進度合併失敗會遺留抽出原音；擴大 try/catch 清理範圍後 3/3 PASS。

## 拆批結果

- Domain：182/182 PASS。
- App：187/187 PASS；以 editor／arrangement／responsive、practice／library／shared、export／labeling／progress／e2e 三批執行，新增解包故障測試後另做 targeted 重跑。
- Infra：95 PASS＋1 項因未設 `FFMPEG_PATH` 正常 SKIP；真 AnalysisPipeline 2/2 PASS；真 Demucs 1/1 PASS（約 2 分 7 秒）。
- Analyze：`flutter analyze`，No issues。
- Guardrails：v1.1 PASS（IMPLEMENTED 15、PARTIAL 10）；v1 baseline PASS。
- 授權：25 個元件白名單 PASS；`scripts/test_check_licenses.py` 與 `scripts/test_prepare_release_sidecars.py` 10/10 PASS。
- `git diff --check`：PASS。
- 誠實揭露：依本環境單次執行上限拆批；本輪未執行整條 `bash scripts/ci_core_checks.sh`，不宣稱該單條指令通過。

## Release 與容量

- `flutter build macos --release` 成功；產物為 `app/build/macos/Build/Products/Release/syllable_repeater_app.app`（build 顯示 634.9MB，`du` 606M）。
- 增量 build 後外層 resource seal 需更新；沿用 `Release.entitlements` ad-hoc 深層重簽，`codesign --verify --deep --strict` PASS。
- 已用 `open -n` 啟動最新 Release；啟動成功不等於 Finder／真人操作驗收完成。
- `.app` 內 sidecar 581M：Whisper small.en 模型 465M、Demucs 模型 80M；這是固定隨 App 發布的模型，不會因匯入音檔而增長。
- 目前找到舊版未管理 temp 218M 與舊 `syllable_repeater_steps` 80M。新 session 生命週期避免後續累積；既有 298M 尚未刪除，待關閉所有 App 後取得使用者一次性清理批准。

## 仍需人工驗收

- 在 Release App 快速連續新增／刪除／拖曳切點，確認節點即時且舊粉線不殘留。
- 1100×700 下將最外層捲到頂，再把自由排列列區捲到底，確認最後一列完整且不跳回倒數第二列。
- 以 20 列實測來源積木拿起、放開觸控板、捲到遠端列後放下，確認命中與取消手感。
- 真人麥克風、錄音回放及既有 S9-20／S9-21 Finder 項目仍未由使用者驗收。

## 人工驗收補記（2026-07-15）

使用者明示「真人驗收OK」，本報告原列的切點延遲、末列捲動、長距離放置、錄音回放及既有 Finder 項目均已通過。
