// AI-Generate
# S9-23 自由排列互動簡化測試報告（2026-07-15）

## 範圍

- 既有積木只允許同列長按本體排序、成組、組內排序／抽出或單一積木併入組合；跨列操作由 UI 與 Domain 雙層拒絕。
- 移除六點拖曳把手、大型藍色跨列插入軌與積木麥克風裝飾。
- 「來源積木」改為「來源段落」；來源只做點選，再以每個頂層積木左側、列尾或空列按鈕插入。
- 移除來源拖曳與邊緣自動捲動，保留列區獨立垂直捲動與新增列定位。

## TDD 證據

- Domain 紅燈：新增跨列 `moveBlock`、`extractGroupedSyllable`、`moveSingleBlockIntoGroup` 拒絕測試，首次 28 PASS／2 FAIL，確認舊實作仍會跨列改資料。
- App 紅燈：新增本體長按、無把手／麥克風、來源按鈕插入、來源非 Draggable 與長列捲動測試，首次 12 PASS／10 FAIL。
- 最小修正後 targeted：`practice_arrangement_test.dart` 30/30 PASS；`app/test/arrangement` 26/26 PASS。

## 拆批結果

- Domain：184/184 PASS。
- App：189/189 PASS；拆為 arrangement、editor/export/features、labeling/library/pack、practice/progress/shared、root e2e 五批循序執行。
- Infra：95 PASS＋1 項未設定 `FFMPEG_PATH` 正常 SKIP；真 Demucs 整合測試耗時約 2 分 27 秒後完成。
- Analyze：`flutter analyze`，No issues。
- Guardrails：v1.1 PASS（IMPLEMENTED 15、PARTIAL 10）；v1 baseline PASS。
- 授權：25 個元件白名單 PASS；授權／release sidecar Python 測試 10/10 PASS。
- `git diff --check`：PASS。
- 誠實揭露：依環境限制拆批執行，未把未執行的單條 `bash scripts/ci_core_checks.sh` 宣稱為通過。

## Release 與畫面來源

- `flutter build macos --release` 成功，產出 `app/build/macos/Build/Products/Release/syllable_repeater_app.app`（634.9MB）。
- `codesign --verify --deep --strict` PASS；產物為 x86_64、ad-hoc 簽章。
- 已以 `open -n` 啟動最新 Release，並擷取真實 Release 視窗至 `social-assets/source/release-app-home.png`。
- 原規劃以真實 App 畫面製作兩張社群圖；內建產圖服務連續回傳 HTTP 520，使用者隨後明示取消製圖，因此本輪未產出社群圖，也未改走需 API key 的 CLI 路徑。

## 仍需人工驗收

- Finder App 內以滑鼠長按與 macOS 系統三指拖移，確認同列排序／成組命中手感。
- 點選來源段落後，實測第一個、組合前與列尾插入；確認列區捲動不再被來源拖曳打斷。
- S9-20 錄音、S9-21／22 既有 Finder 手感與波形項目仍待使用者驗收。

## 人工驗收補記（2026-07-15）

使用者明示「真人驗收OK」，本報告原列的長按／三指同列操作、來源段落插入、錄音與波形關聯項目均已通過；已取消的社群圖不列入驗收。
